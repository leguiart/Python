sum=0
i=0
num=0
maxnum=None
minnum=None
while True:
    if num!="done":   
        num= raw_input("Enter number, exit with done:")
        try:
            num= float(num)
            if maxnum is None:            
                maxnum= num
                minnum= num
            elif num>maxnum:
                if i==0:
                    minnum= maxnum
                    maxnum= num
                else:
                    maxnum= num
            elif num<minnum:
                minnum= num
                
        except:
            if num=="done":
                continue
            else:
                print "Invalid input"                    
    elif num== "done":
        break
    i=1
print "Average:",num
    
    